#include <stdio.h>

int main() {
    int a,b = 0;

    printf("Enter a positive integer: ");
    scanf("%d", &a);

    for (int i = 1; i <= n; i++) {
        b += i;
    }

    printf("Sum of the numbers is: %d\n",b);

    return 0;
}
